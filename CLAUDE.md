## Project

Type: html
Deploy: github-pages

## Structure

index.html, src/styles.css, src/script.js, posts/, assets/images/

## Rules

- No build step, no npm
- Each blog post is a standalone HTML file under posts/
- CSS vars from settings widget, never hardcode colors
- MUST have favicon.svg + favicon.png + favicon.ico in assets/images/
